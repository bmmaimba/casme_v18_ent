<?xml version="1.0" encoding="UTF-8"?>
<odoo>
        <!-- <record id="action_project_programmes" model="ir.actions.act_window">
            <field name="name">Project Programmes</field>
            <field name="res_model">project.project</field>
            <field name="view_mode">list,form,kanban,pivot,graph</field>
            <field name="context">{}</field>
            <field name="limit">50</field>
			<field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">Create New Project Programmes</p>
			</field>
        </record> -->

    <!-- project.project Child Menus -->
    <menuitem id="menu_casme_project_programmes" name="Projects &amp; Programmes" parent="menu_casme" action="project.open_view_project_all_config_group_stage" sequence="8"/>

</odoo>